{% extends "PWMainBundle::layout.html.twig" %}

{% block body %}

	<h3>Formulaire de création de groupe</h3>

	<div class="well">
		{{form_start(form,{'attr':{'class': 'form-horizontal'}})}}

		{{form_errors(form)}}

		<div class="form-group">

		{{form_label(form.titre,"Nom du groupe",{'label_attr': {'class':'col-sm-2 control-label'}})}}
		{{form_widget(form.titre,{'attr': {'class' : 'form-control'}})}}

		{{form_label(form.description,"Description du groupe",{'label_attr': {'class':'col-sm-2 control-label'}})}}
		{{form_widget(form.description,{'attr': {'class' : 'form-control'}})}}

		 {{ form_widget(form.save, {'attr': {'class': 'btn btn-primary'}}) }}
		{{form_rest(form)}}

		{{form_end(form)}}

	</div>

{% endblock %}
